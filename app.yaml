name: YAT
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/YAT
  original_url: https://huggingface.co/spaces/Chris4K/YAT

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
